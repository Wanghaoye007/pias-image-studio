# 图片工作台 MVP 发布前收口

## 范围

本轮只验收图片工作台，不包含视频、短剧、数字人或其他工作台。目标链路为：

`登录 -> 创建项目 -> 上传图片 -> 放入画布 -> 选择操作 -> 配置任务 -> 提交生成 -> 进度 -> 结果 -> 继续生成 -> 采用 -> 审核或下载 -> 用量记录`

## 审计结论

### P0 核心链路阻塞

| 问题 | 收口前状态 | 本轮处理 | 结果 |
| --- | --- | --- | --- |
| 创建项目后仍停留在项目列表 | 交互断点 | 创建成功后直接打开新项目 | 已修复 |
| 工作台不能直接上传并入画布 | 只能绕到素材库 | 抽取真实上传对话框，上传后自动创建画布节点 | 已修复 |
| 空白占位节点与首张素材并存 | 画布焦点错乱 | 首张素材替换无内容占位节点 | 已修复 |
| 取消接口失败仍在前端显示已取消 | 伪成功 | 校验 HTTP 结果，供应商未确认时恢复原状态和冻结额度 | 已修复 |
| 队列恢复失败后仍可提交新任务 | 可隐藏恢复错误并重复提交 | 返回 `FAL_PERSIST_FAILED` 503，不调用上游提交 | 已修复 |
| 结果无法直观追溯原图和父结果 | 血缘只存在数据中 | 结果详情展示原始图片、版本和上一级结果 | 已修复 |
| 结果详情被选中的画布节点遮挡 | 详情不可稳定操作 | 建立独立堆叠上下文并增加浏览器点位验收 | 已修复 |
| 无核心链路集成与浏览器 E2E | 只有分散单测 | 新增域集成测试和基于真实 `dist` 的有状态 Playwright 验收 | 已修复 |

### P1 重要但不阻塞图片链路

| 问题 | 处理或当前状态 |
| --- | --- |
| 底层模型名称占据用户主信息 | 已弱化；默认展示生成、修改光影、融图、扩图、多角度、去除、抠图、超分等用户任务，技术 ID 收入折叠追踪 |
| 输出数量的动效过渡可造成短暂视觉歧义 | 已修复高亮规则，E2E 等待计费与视觉状态同时稳定 |
| Fal 供应商账单 Admin API | 未完成真实凭证验证；当前凭证历史预检返回 403，不得将发布预检标记为全绿 |
| 任务返回本地 request ID 后、StudioState 首次自动保存完成前的极短窗口 | 服务端队列仍持久化，但页面立即崩溃时可能缺少 Studio Job 映射；建议后续改为服务端原子创建映射 |

### P2 后续优化

| 问题 | 当前状态 |
| --- | --- |
| 小屏移动端不提供完整节点编辑 | 保留结果查看与状态交互，完整编辑仍定位桌面工作台 |
| 局域网邮件 Relay 和真实收件 | 与图片核心链路无关，但整体 acceptance 仍会保持 `partial`，直到生产邮件配置完成 |

## 界面收口

- 画布是主视觉区，左侧保留素材和上传，右侧为当前任务编辑面板。
- 右侧面板从画布顶部贯穿到底部，打开后画布可用区同步收缩。
- Prompt 输入区支持纵向拖拽，最小高度 96px，最大高度 45vh。
- 结果详情使用独立顶层，画布节点不能覆盖其交互区。
- 验收截图：`docs/acceptance/evidence/image-mvp-workbench-editor-2026-07-23.png`
- 血缘截图：`docs/acceptance/evidence/image-mvp-result-lineage-2026-07-23.png`

## 自动化证据

- 域集成测试：`tests/imageMvpCoreFlow.integration.test.ts`
- 结果追溯组件测试：`tests/resultInspector.test.tsx`
- 浏览器 E2E：`scripts/image-mvp-e2e.mjs`
- E2E 覆盖创建项目、上传入画布、提交、刷新恢复、继续生成、采用、血缘、审核、导出和用量记录，并断言 Fal 只提交 2 次，刷新不重复提交。

## 最终门禁

| 门禁 | 结果 | 证据 |
| --- | --- | --- |
| `npm run repo:check` | 通过 | 208 个已跟踪文件通过仓库卫生检查 |
| `npm run typecheck` | 通过 | TypeScript project references 无错误 |
| `npm run lint` | 通过 | ESLint `--max-warnings 0` |
| `npm test` | 通过 | 54 个测试文件、446 条测试 |
| `npm run build` | 通过 | 客户端 `dist` 和独立服务端 `dist-server` 均构建成功 |
| `npm run e2e:image-mvp` | 通过 | 2 次 Fal 提交生成 2 个结果，刷新后无重复提交 |
| `npm run acceptance:report` | 黄色 | 17 项通过、2 项部分通过；自动化无失败 |
| `npm run release:preflight:report` | 未放行 | 干净提交构建、新鲜备份、数据库完整性、回滚、身份、TLS、持久化和服务端产物通过；仅剩 `FAL_KEY_FILES_REQUIRED`、`EMAIL_CONFIG_REQUIRED`、`BILLING_ADMIN_KEY_REQUIRED` |

Acceptance 保持黄色的原因与图片核心用户链路无关，但对“正式生产上线”仍是真实风险：

1. `USAGE-001`：缺少可通过 Fal Billing Events 的生产 Admin Key，供应商成本对账未实地验证。
2. `MEMBER-001`：邮件 Relay、生产域名和真实收件验证未配置。
